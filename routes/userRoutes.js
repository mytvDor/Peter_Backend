const express = require('express');
const authMiddleware = require('../middleware/authMiddleware');
const router = express.Router();

// Add user-related routes here, e.g., profile management

module.exports = router;
